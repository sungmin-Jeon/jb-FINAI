---
config:
  flowchart:
    curve: linear
---
graph LR;
	__start__([<p>__start__</p>]):::first
	ORCHESTRATOR_NODE(ORCHESTRATOR_NODE)
	TRIAGE_NODE(TRIAGE_NODE)
	PREDICTION_NODE(PREDICTION_NODE)
	KG_RETRIEVAL_NODE(KG_RETRIEVAL_NODE)
	RETRIEVAL_NODE(RETRIEVAL_NODE)
	KG_EXPANSION_NODE(KG_EXPANSION_NODE)
	JUDGMENT_NODE(JUDGMENT_NODE)
	REWRITE_NODE(REWRITE_NODE)
	VERIFICATION_NODE(VERIFICATION_NODE)
	COMPARATOR_NODE(COMPARATOR_NODE)
	REPORT_NODE(REPORT_NODE)
	QA_NODE(QA_NODE)
	__end__([<p>__end__</p>]):::last
	COMPARATOR_NODE --> REPORT_NODE;
	JUDGMENT_NODE -.-> REPORT_NODE;
	JUDGMENT_NODE -.-> REWRITE_NODE;
	KG_EXPANSION_NODE --> JUDGMENT_NODE;
	KG_RETRIEVAL_NODE --> RETRIEVAL_NODE;
	ORCHESTRATOR_NODE -. &nbsp;qa&nbsp; .-> QA_NODE;
	ORCHESTRATOR_NODE -. &nbsp;review&nbsp; .-> TRIAGE_NODE;
	PREDICTION_NODE --> KG_RETRIEVAL_NODE;
	RETRIEVAL_NODE --> KG_EXPANSION_NODE;
	REWRITE_NODE --> VERIFICATION_NODE;
	TRIAGE_NODE --> PREDICTION_NODE;
	VERIFICATION_NODE -.-> COMPARATOR_NODE;
	VERIFICATION_NODE -.-> REWRITE_NODE;
	__start__ --> ORCHESTRATOR_NODE;
	QA_NODE --> __end__;
	REPORT_NODE --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
